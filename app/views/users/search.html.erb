<section id="table" class="section-with-bg">
  <div class="container aos-init aos-animate" data-aos="fade-up">
    <div class="section-header">
      <h2 class="mt-5">Search Result</h2>
      <p>ユーザー検索結果一覧</p>
    </div>
    <section id="search-form">
      <div class="container aos-init aos-animate" data-aos="zoom-in">
        <%= form_with(url: search_users_path, local: true, method: :get ) do |f| %>
          <div class="row justify-content-center">
            <div class="col-lg-6 col-md-10 d-flex">
              <%= f.text_field :keyword, class:"form-control", placeholder:"ユーザー名を入力" %>
              <%= f.button "Search", :type => "submit", class:"ms-2" %>
            </div>
          </div>
        <% end %>
      </div>
    </section>
    <%= render 'users/users_table', users: @searched_users, current_user: @current_user, users_count: @searched_users_count %>
  <div>
</section>